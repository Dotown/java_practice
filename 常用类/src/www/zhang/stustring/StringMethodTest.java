package www.zhang.stustring;

/**
 * length
 * charAt(int index)
 * boolean isEmpty()
 * toLowerCase()
 * toUpperCase()
 * trim()去除首位空格
 * equals()
 * equalsIgnoreCase
 * cancat 等价于"+"
 * compareTo比较大小 负数，当前对象小
 * substring(beginIndex,endIndex)左闭右开
 *
 *
 * boolean endsWith(String suffix)
 * boolean startsWith(String prefix)
 * boolean startsWith(String prefix, int toffset) //c从指定索引开始的子字符串是否以指定前缀开始
 * boolean contains(CharSequence s)字符串包含指定的char值序列时，返回true
 * int indexOf(String str)返回指定子字符串中第一次出现处的索引
 * int indexOf(String str, int fromIndex)返回指定子字符串在此字符串中第一次出现的索引，从指定的索引开始
 * int lastIndexOf(String str)最后往前找，返回的int还是从前往后的索引
 * int lastIndexOf(String str, int fromIndex) 返回指定子字符串在此字符串中最后一次出现的索引，从指定的索引开始
 *
 *
 * String replace(char oldChar, char newChar)
 * String replace(CharSequence target, CharSequence replacement)替换字符串
 * String replaceAll(String regex, String replacement)regex是正则表达式
 * String replaceFirst(String regex, String replacement)
 * boolean matches(String regex)
 * String[] split(String regex)
 * String[] split(String regex, int limit)
 *
 *
 * @author Dotown
 * @create 2020-12-25-14:39
 */
public class StringMethodTest {
}
